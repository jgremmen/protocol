pluginManagement {
  repositories {
    gradlePluginPortal()
  }
}


include 'protocol-core'
include 'protocol-html'
include 'protocol-message-matcher'


rootProject.name = 'protocol'
rootProject.children.each {project ->
  project.buildFileName = "${project.name}.gradle"
}
